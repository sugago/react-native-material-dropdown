import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingVertical: 8,
    borderRadius: 0,
    justifyContent: 'flex-start',
    flexDirection: 'row',
    paddingLeft: 20,
  },
});
